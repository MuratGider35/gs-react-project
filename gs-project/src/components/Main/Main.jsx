import React from "react";
import "./Main.scss";

const Main = ({data}) => {
   
  return <div className="card">
    <p>{title}</p>

  </div>;
};

export default Main;
const { id, title, desc, image } = props